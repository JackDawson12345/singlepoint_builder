<div class="">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="nata-sans page-title">Products</h1>
      <p class="nata-sans font-normal text-base">A list of all products on your website including their name, image, status and Inventory.</p>
    </div>
    <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
      <%= link_to "Add Product", new_manage_website_shop_product_path, class: "nata-sans inline-flex items-center justify-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto" %>
    </div>
  </div>

  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
          <table class="min-w-full divide-y divide-gray-300">
            <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="nata-sans py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-6">Product</th>
              <th scope="col" class="nata-sans px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Status</th>
              <th scope="col" class="nata-sans px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Price</th>
              <th scope="col" class="nata-sans px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Category</th>
              <th scope="col" class="nata-sans px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Created</th>
              <th scope="col" class="nata-sans px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Actions</th>
            </tr>
            </thead>
            <!-- In your index.html.erb view, update the loop to use @products_with_images -->
            <tbody class="divide-y divide-gray-200 bg-white">
            <% @products.each do |product| %>
              <tr class="hover:bg-gray-50">
                <td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-6">
                  <div class="flex items-center">
                    <div class="h-10 w-10 flex-shrink-0">
                      <% if product['images'].present? && product['images'].any? %>
                        <%= image_tag product['images'].first, class: "h-10 w-10 rounded-full object-cover" %>
                      <% else %>
                        <div class="h-10 w-10 rounded-full bg-blue-500 flex items-center justify-center">
                          <svg class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                          </svg>
                        </div>
                      <% end %>
                    </div>
                    <div class="ml-4">
                      <div class="text-sm font-medium text-gray-900 nata-sans"><%= product['data']['name'] %></div>
                      <% if product['description'].present? %>
                        <div class="text-sm text-gray-500 nata-sans"><%= truncate(product['data']['description'], length: 50) %></div>
                      <% end %>
                      <% if product['images'].length > 1 %>
                        <div class="text-xs text-gray-400 nata-sans"><%= product['images'].length %> images</div>
                      <% end %>
                    </div>
                  </div>
                </td>
                <!-- rest of your table columns -->
                <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                  <span class="nata-sans inline-flex rounded-full px-2 text-xs font-semibold leading-5 bg-green-100 text-green-800">
                    Active
                  </span>
                </td>
                <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                  <span class="nata-sans inline-flex rounded-full px-2 text-xs font-semibold leading-5">
                    £<%= number_with_precision(product['price']['price'], precision: 2) %>
                  </span>
                </td>
                <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                  <div class="flex space-x-1">
                    <%= product['data']['category'] || 'Uncategorized' %>
                  </div>
                </td>
                <td class="nata-sans whitespace-nowrap px-3 py-4 text-sm text-gray-500">
                  <%= Date.parse(product['created_at']).strftime("%b %d, %Y") if product['created_at'] %>
                </td>
                <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
                  <%= link_to "Edit", edit_manage_website_shop_product_path(product['id']),
                              class: "text-indigo-600 hover:text-indigo-900 nata-sans text-sm font-medium" %>
                  <%= link_to "Delete", manage_website_shop_product_path(product['id']),
                              method: :delete,
                              data: { confirm: 'Are you sure you want to delete this product?' },
                              class: "ml-2 text-red-600 hover:text-red-900 nata-sans text-sm font-medium" %>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <% if @products.empty? %>
    <div class="text-center py-12">
      <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
      </svg>
      <h3 class="nata-sans mt-2 text-sm font-medium text-gray-900">No Product</h3>
      <p class="nata-sans mt-1 text-sm text-gray-500">Get started by creating a new Product.</p>
      <div class="mt-6">
        <%= link_to "Add Product", new_manage_website_shop_product_path, class: "nata-sans inline-flex items-center justify-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto" %>
      </div>
    </div>
  <% end %>
</div>