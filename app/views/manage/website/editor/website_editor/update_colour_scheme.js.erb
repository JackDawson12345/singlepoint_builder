// Update the main content area
document.querySelector('.w-19\\/20').innerHTML = '<%= j(render partial: "page_content") %>';

// Hide the sidebar
hideSidebar();

// Show success message (optional)
// You can add a toast notification here if you have one