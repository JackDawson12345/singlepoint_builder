<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Manage Website - Website Builder" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <!-- Dynamic favicon based on current website -->
  <% if current_website&.favicon&.attached? %>
    <%= favicon_link_tag url_for(current_website.favicon) %>
    <link rel="apple-touch-icon" href="<%= url_for(current_website.favicon) %>">
  <% else %>
    <!-- Fallback to default favicons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
  <% end %>

  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

</head>

<body class="" style="margin: 0px;">
<%= yield %>
</body>
</html>